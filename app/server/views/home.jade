!!! 5
html.no-js(lang='en')
//
    <![endif]
  head
    meta(charset='utf-8')
    title Audiee
    meta(name='description', content='Simple experimental web based audio editor.')
    meta(name='author', content='Jan Myler')
    link(rel='author', href='humans.txt')
    link(rel='shortcut icon', href='favicon.ico')
    meta(http-equiv='X-UA-Compatible', content='IE=edge,chrome=1')
    meta(name='viewport', content='width=device-width')
    link(rel='stylesheet/less', href='less/style.less')
    script(src='js/libs/less-1.2.2.js')
    script(src='js/libs/tuna.js')
    //
       Use SimpLESS (Win/Linux/Mac) or LESS.app (Mac) to compile your .less files
          to style.css, and replace the 2 lines above by this one: 
    //
       <link rel="stylesheet" href="css/style.css"> 
  body

    // preset form values if we receive a userdata object //
    - user = typeof(udata) != 'undefined' ? udata : { }
    // store the userId on the client side in a hidden input field //

    - if(typeof(udata) == 'undefined') {
      input(type='hidden', value='guest')#userId
    - } else {
      input(type='hidden', value= user_id)#userId
    - }


    .navbar.navbar-fixed-top
      .navbar-inner
        .container-fluid
          #project-name.pull-left.editable
          #playback-controls.btn-toolbar
            input#time-display(type='text', value='-:--:--.---')
            button#play.btn(title='play')
              i.icon-play
            button#stop.btn(title='stop')
              i.icon-stop
            button#follow.btn.following(title='toggle follow')
              i.icon-eye-open
              i.icon-eye-close
            button#save.btn.following(title='save')
              i.icon-ok

        .app-name
          - if(typeof(udata)=='undefined'){
          -}
          
    #menu-view.menubar.menubar-fixed
      ul.nav.nav-pills
    #app-frame.container-fluid
      .row-fluid
        #editor-view.span12
          .inner
            #time-line
            #tracks
        //
           /div#editor-view 
    //
       /div#app-frame 
    //
       JavaScript at the bottom for fast page loading 
    script(data-main='js/main', src='js/libs/require/require.js')
